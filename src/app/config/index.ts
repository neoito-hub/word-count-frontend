export const BASE_URL = "http://localhost:3000";

export const POST_SENTENCE_URL = `${BASE_URL}/freq`;
export const GET_WORDS_LIST_URL = `${BASE_URL}/user`;
export const USER_URL = `${BASE_URL}/user`;

export const LOCALSTORAGE_KEYS = {
  userId: "user_id"
};
